type ListItem = {
    __$originalPosition?: UTSSourceMapPosition<"ListItem", "pages/template/custom-tab-bar/custom-tab-bar-tab1.uvue", 15, 8>;
    title: string;
};
const __sfc__ = defineComponent({
    data() {
        return {
            dataList: [] as ListItem[],
            oldScrollTop: 0,
            newScrollTop: 0
        };
    },
    created() {
        this.loadData();
        // console.log("tab1 created");
    },
    methods: {
        loadData() {
            let index = this.dataList.length;
            for (let i = 0; i < 20; i++) {
                this.dataList.push({
                    title: index.toString(10),
                } as ListItem);
                index++;
            }
        },
        onScroll(e: ScrollEvent) {
            uni.$emit('tabchange', e.detail.scrollTop);
            this.oldScrollTop = e.detail.scrollTop;
        },
        scrollTop(top: number) {
            // (this.$refs["listView"] as UniElement).scrollTop = top
            // console.log("tab1 to top");
            this.newScrollTop = this.oldScrollTop;
            this.$nextTick(() => {
                this.newScrollTop = top;
            });
        }
    }
});
export default __sfc__;
function GenPagesTemplateCustomTabBarCustomTabBarTab1Render(this: InstanceType<typeof __sfc__>): any | null {
    const _ctx = this;
    const _cache = this.$.renderCache;
    return _cE("scroll-view", _uM({
        ref: "listView",
        class: "list",
        bounces: false,
        "scroll-with-animation": true,
        "scroll-y": true,
        "scroll-top": _ctx.newScrollTop,
        onScrolltolower: () => { _ctx.loadData(); },
        onScroll: _ctx.onScroll
    }), [
        _cE(Fragment, null, RenderHelpers.renderList(_ctx.dataList, (item, index, __index, _cached): any => {
            return _cE("view", _uM({
                class: "list-item",
                key: index
            }), [
                _cE("view", _uM({ class: "cell-item" }), [
                    _cE("text", _uM({ class: "title" }), "内容：" + _tD(item.title), 1 /* TEXT */),
                    _cE("input", _uM({
                        class: "title",
                        style: _nS(_uM({ "margin-top": "8px" })),
                        placeholder: "备注:"
                    }), null, 4 /* STYLE */)
                ])
            ]);
        }), 128 /* KEYED_FRAGMENT */)
    ], 40 /* PROPS, NEED_HYDRATION */, ["scroll-top", "onScrolltolower", "onScroll"]);
}
const GenPagesTemplateCustomTabBarCustomTabBarTab1Styles = [_uM([["list", _pS(_uM([["flexGrow", 1], ["flexShrink", 1], ["flexBasis", "0%"], ["backgroundColor", "#ffffff"]]))], ["list-item", _pS(_uM([["flexDirection", "row"], ["paddingTop", 30], ["paddingRight", 30], ["paddingBottom", 30], ["paddingLeft", 30], ["borderBottomWidth", 1], ["borderBottomStyle", "solid"], ["borderBottomColor", "#dbdbdb"]]))], ["title", _pS(_uM([["fontSize", 16], ["textAlign", "left"]]))], ["cell-item", _pS(_uM([["display", "flex"], ["flexDirection", "column"]]))]])];
//# sourceMappingURL=custom-tab-bar-tab1.uvue.map